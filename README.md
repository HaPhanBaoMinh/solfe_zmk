# solfe_zmk

## Layouts

The `main` branch builds the Sofle keymap. The `corne` branch builds the Corne 3x6 layout (`corne_left` and `corne_right` shields). Both layouts use momentary `LOWER`/`RAISE` layers and expose `ADJUST` while both are held.
